alias ll='ls -l'
alias la='ls -la'

alias gs='git status'
alias ga='git add .'
alias gc='git commit'

eval "$(hub alias -s)"

export NVM_DIR="/Users/brandonhorst/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
